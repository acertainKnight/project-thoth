# Docker environment for Thoth with new .thoth structure
# Set this to point to your Obsidian vault's .thoth directory
THOTH_DATA_MOUNT=/home/nick/python/project-thoth/.thoth

# Or use a dedicated directory for Docker-only usage
# THOTH_DATA_MOUNT=./docker-thoth-data

# Other settings remain the same...
DOCKER_ENV=true
THOTH_WORKSPACE_DIR=/workspace
