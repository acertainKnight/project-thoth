# Thoth Environment Configuration Example
# Copy this file to .env and fill in your actual values.
# This file contains both Docker and development environment settings.
# API keys and detailed settings are configured in .thoth.settings.json

# ----------------------------------------------------------------------------------
# --- Environment Settings ---
# ----------------------------------------------------------------------------------

# Environment type (docker/local/dev)
ENVIRONMENT=docker

# Python settings
PYTHONUNBUFFERED=1
PYTHONPATH=/app/src

# Development mode (set to true for development with hot reload)
DEVELOPMENT=false

# ----------------------------------------------------------------------------------
# --- API Keys (Development) ---
# ----------------------------------------------------------------------------------
# Note: For production, use secure secrets management
# For local development, uncomment and fill in your API keys:

# OPENAI_API_KEY=your-openai-api-key-here
# ANTHROPIC_API_KEY=your-anthropic-api-key-here
# API_MISTRAL_KEY=your-mistral-api-key-here
# API_OPENROUTER_KEY=your-openrouter-api-key-here
# API_SEMANTIC_SCHOLAR_KEY=your-semantic-scholar-api-key-here
# API_WEB_SEARCH_KEY=your-web-search-api-key-here

# ----------------------------------------------------------------------------------
# --- Docker Network and Service Configuration ---
# ----------------------------------------------------------------------------------

# ChromaDB connection (when using external ChromaDB container)
THOTH_CHROMADB_HOST=chromadb
THOTH_CHROMADB_PORT=8003
THOTH_CHROMADB_URL=http://chromadb:8003

# Letta memory service connection (when using external Letta container)
LETTA_SERVER_HOST=0.0.0.0
LETTA_SERVER_PORT=8283
LETTA_PG_URI=postgresql://letta:letta_password@letta-postgres:5432/letta

# Letta security and configuration
LETTA_SERVER_PASSWORD=letta_dev_password

# Development database passwords (change for production)
POSTGRES_PASSWORD=letta_dev_password
REDIS_PASSWORD=redis_dev_password

# ----------------------------------------------------------------------------------
# --- Docker Volume Paths ---
# ----------------------------------------------------------------------------------

# Workspace directory inside container
THOTH_WORKSPACE_DIR=/workspace
THOTH_PDF_DIR=/workspace/pdfs
THOTH_NOTES_DIR=/workspace/notes
THOTH_DATA_DIR=/workspace/data
THOTH_QUERIES_DIR=/workspace/queries
THOTH_DISCOVERY_SOURCES_DIR=/workspace/discovery
THOTH_KNOWLEDGE_BASE_DIR=/workspace/knowledge
THOTH_LOGS_DIR=/workspace/logs
THOTH_CACHE_DIR=/workspace/cache

# ----------------------------------------------------------------------------------
# --- Docker Server Binding ---
# ----------------------------------------------------------------------------------

# API server binding (bind to all interfaces in container)
THOTH_API_HOST=0.0.0.0
THOTH_API_PORT=8000

# MCP server binding
THOTH_MCP_HOST=0.0.0.0
THOTH_MCP_PORT=8001

# ----------------------------------------------------------------------------------
# --- Docker Development Features ---
# ----------------------------------------------------------------------------------

# Enable hot reload in development containers
THOTH_ENABLE_HOT_RELOAD=0

# Development mode features
THOTH_DEV_MODE=0

# ----------------------------------------------------------------------------------
# --- Service URLs (Docker Compose) ---
# ----------------------------------------------------------------------------------
# These are automatically configured by docker-compose
# THOTH_CHROMADB_URL=http://chromadb:8003
# THOTH_LETTA_URL=http://letta:8283
# THOTH_REDIS_URL=redis://redis:6379

# ----------------------------------------------------------------------------------
# --- Discovery Settings ---
# ----------------------------------------------------------------------------------
# Discovery Settings
THOTH_DISCOVERY_AUTO_START_SCHEDULER=false
THOTH_DISCOVERY_DEFAULT_MAX_ARTICLES=25
THOTH_DISCOVERY_CHECK_INTERVAL=300

# ----------------------------------------------------------------------------------
# --- Agent Settings ---
# ----------------------------------------------------------------------------------
# Agent Settings
THOTH_RESEARCH_AGENT_MEMORY_ENABLED=true
THOTH_AGENT_MAX_TOOL_CALLS=20
THOTH_AGENT_TIMEOUT_SECONDS=300
THOTH_AGENT_CONCURRENT_SESSIONS=2

# ----------------------------------------------------------------------------------
# --- PDF Monitor Settings ---
# ----------------------------------------------------------------------------------
# PDF Monitor Settings
THOTH_PDF_MONITOR_INTERVAL=30
THOTH_PDF_MONITOR_BATCH_SIZE=3

# ----------------------------------------------------------------------------------
# --- Security (Development) ---
# ----------------------------------------------------------------------------------
# Security (Development - change for production)
THOTH_API_SECRET_KEY=dev-secret-key-not-for-production
THOTH_CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# Docker flag (indicates running in container)
THOTH_DOCKER=1

# ----------------------------------------------------------------------------------
# --- Docker Logging ---
# ----------------------------------------------------------------------------------

# Log level (can be overridden for container debugging)
THOTH_LOG_LEVEL=INFO
